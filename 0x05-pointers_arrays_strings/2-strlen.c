#include "holberton.h"
#include <stdio.h>
#include<string.h>
/**
 * _strlen - give the length of a string
 * @s: the string
 *
 * Return: the length of a string
 */
int _strlen(char *s);
{
printf"(%d/n", strlen(*s));
return (0);
}
