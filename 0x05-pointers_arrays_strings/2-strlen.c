#include "holberton.h"
#include <stdio.h>
/**
 * _strlen - give the length of a string
 * @s: the string
 *
 * Return: the length of a string
 */
int _strlen(char *s);
i == 0;
while (*s++)
i++;
return (i);
}
